<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/actionbar_background</item>
        <item name="colorPrimaryDark">@color/actionbar_title</item>
        <item name="colorAccent">@color/edittext_stroke_default</item>
        <item name="actionBarTheme">@style/actionbar_style</item>
        <item name="android:windowBackground">@color/window_background</item>
    </style>
    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="actionbar_style" parent="ThemeOverlay.AppCompat.ActionBar">
        <!--<item name="android:background">@color/actionbar_background</item>-->
        <!--<item name="android:windowContentOverlay">@null</item>-->
        <item name="elevation">1px</item>

    </style>
    <style name="actionbar_style.transparent" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="actionMenuTextColor">#fff</item><!-- actionbar buttons text color -->
        <item name="android:textColorPrimary">#fff</item><!-- actionbar text color -->
        <item name="android:textColorSecondary">#fff</item><!-- actionbar buttons color -->
    </style>
</resources>
